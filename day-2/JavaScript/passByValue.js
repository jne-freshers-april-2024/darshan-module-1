let number=4;

function cube(b){
    return b*b*b;
}

let result=cube(number);  // pass number value in function and proccess it

console.log(result);  // after processing the value of number variable value

console.log(number);  // number value is not change